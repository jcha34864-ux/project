---
description: Feature implementation unit — 밥 같이 먹을 사람을 매칭하려면 두 사람의 음식 취향이 비슷한지를 알아야해서 앱 가입하면 자신이 좋아하는 음식을 고르게 하고 그 데이터를 기반으 (혼자밥먹는 사람을 위한 밥 같이 먹기). Load only while implementing this feature.
globs: ["app/**", "frontend/**", "backend/**", "src/**"]
alwaysApply: false
---

# Feature: 밥 같이 먹을 사람을 매칭하려면 두 사람의 음식 취향이 비슷한지를 알아야해서 앱 가입하면 자신이 좋아하는 음식을 고르게 하고 그 데이터를 기반으

- **english_key**: `screen-1f6665`
- **priority**: must
- **scope**: Implement ONLY this feature slice in this session.

## TDD mandate

1. Write failing tests first (unit or integration) for the acceptance criteria below.
2. Implement minimal code to pass tests.
3. Do NOT read the entire PRD — use this file + referenced routes/entities only.

## Acceptance (compressed)

- **When** 주요 기능을 사용했을 때 → **Then** 7일 재방문율 30% 이상

## Detail

밥 같이 먹을 사람을 매칭하려면 두 사람의 음식 취향이 비슷한지를 알아야해서 앱 가입하면 자신이 좋아하는 음식을 고르게 하고 그 데이터를 기반으론 매칭하도록 먼저 여러가지 음식 리스트를 뽑고 그걸로 사용자의 취향 데이터를 얻어야해 그리고 같은 음식 취향을 가진 사람들을 매칭하는 식으로

## Stop condition

When all acceptance tests pass and routes match semantic paths in PRD §5, stop and commit.
