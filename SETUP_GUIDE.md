# 🚀 고양이 사진 찍으면 고양이 종 알려주는 앱 셋업 가이드

> 이 문서는 코딩 지식이 없어도 따라 할 수 있도록 작성됐습니다.
> 위에서부터 순서대로 진행하세요. 각 단계 끝의 **✅ 확인**이 통과되면 다음으로 넘어갑니다.
> 막히면 작업지시 화면의 **🆘 막혔어요** 버튼을 눌러 에러 내용을 붙여넣으세요.

---

## 준비물 (5분)

계정 3개가 필요합니다. 전부 무료로 시작할 수 있습니다.

| 계정 | 용도 | 가입 주소 |
|---|---|---|
| GitHub | 내 서비스의 코드가 저장되는 곳 | github.com |
| Supabase | 회원·데이터가 저장되는 곳 (데이터베이스) | supabase.com |
| Vercel | 내 서비스를 인터넷에 띄워주는 곳 | vercel.com |

💡 **팁**: 세 곳 모두 "Continue with GitHub"(GitHub으로 계속하기)를 누르면 GitHub 계정 하나로 전부 가입됩니다. 이 방법을 추천합니다.

**✅ 확인**: 세 사이트에 모두 로그인된 상태

---

## 1단계. GitHub 저장소 확인 (2분)

Cubivora가 이미 코드를 GitHub에 올려두었습니다. 확인만 하면 됩니다.

1. github.com 에 로그인
2. 오른쪽 위 프로필 사진 클릭 → **Your repositories** 클릭
3. **my-project** 저장소가 보이는지 확인. 클릭해서 들어가면 파일 목록이 보입니다

**✅ 확인**: 저장소 안에 `SETUP_GUIDE.md`(지금 이 문서)와 `docs/` 폴더 등이 보임(결제·로그인처럼 아직 준비 안 된 부분이 있으면 `mocks/` 폴더도 함께 보입니다)

---

## 2단계. Supabase 프로젝트 만들기 (5분)

1. supabase.com 로그인 → 초록색 **New project** 버튼 클릭
2. 항목 입력:
   - **Name**: my-project (아무 이름이나 괜찮습니다)
   - **Database Password**: 🔑 **자동 생성된 비밀번호를 복사해서 메모장에 저장하세요.** 나중에 다시 볼 수 없습니다
   - **Region**: `Northeast Asia (Seoul)` 선택 (한국 사용자면 필수)
3. **Create new project** 클릭 → 1~2분 기다리면 준비 완료

### API 키 2개 복사하기

1. 왼쪽 메뉴 맨 아래 ⚙️ **Project Settings** 클릭
2. **API** 메뉴 클릭
3. 다음 두 개를 각각 복사해서 메모장에 저장:
   - **Project URL** — `https://xxxx.supabase.co` 형태
   - **anon public** 키 — `eyJ...`로 시작하는 긴 문자열

⚠️ **주의**: 같은 화면의 `service_role` 키는 절대 외부에 공유하거나 코드에 붙여넣지 마세요. 이 키가 유출되면 데이터베이스 전체가 노출됩니다.

**✅ 확인**: 메모장에 ① DB 비밀번호 ② Project URL ③ anon 키 3개가 저장됨

---

## 3단계. Vercel에 연결하고 인터넷에 띄우기 (5분)

1. vercel.com 로그인 → **Add New...** → **Project** 클릭
2. **Import Git Repository** 목록에서 **my-project** 찾아 **Import** 클릭
   - 목록에 안 보이면: **Adjust GitHub App Permissions** 클릭 → 저장소 접근 허용
3. 배포 설정 화면에서 **Environment Variables**(환경 변수) 섹션을 펼칩니다
4. 아래 표대로 하나씩 추가 (**Key** 칸에 왼쪽, **Value** 칸에 메모장에 저장한 값):

| Key | Value |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | 2단계의 Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 2단계의 anon 키 |
| `USE_MOCK_SANDBOX` | `true` |

💡 `USE_MOCK_SANDBOX=true`는 "아직 준비 안 된 결제·로그인 등을 가짜(연습용)로 돌려라"는 뜻입니다. 덕분에 실제 키가 없어도 서비스가 정상적으로 뜹니다. 실제 연동은 이 문서 맨 뒤 **실전 전환** 챕터에서 합니다.

5. **Deploy** 클릭 → 2~3분 기다림 → 🎉 폭죽 화면이 뜨면 성공

**✅ 확인**: `https://my-project-xxxx.vercel.app` 주소를 클릭했을 때 내 서비스 화면이 보임

❌ **빨간색 에러 화면이 뜬다면**: 화면의 에러 메시지를 전부 복사 → Cubivora 작업지시 화면의 🆘 막혔어요 버튼에 붙여넣기

---

## 4단계. 데이터베이스 테이블 만들기 (3분)

서비스가 데이터를 저장하려면 Supabase에 "표(테이블)"를 만들어야 합니다. 직접 만들 필요 없이, 준비된 명령을 붙여넣기만 하면 됩니다.

1. GitHub 저장소에서 `supabase/schema.sql` 파일 클릭 → 오른쪽 위 복사 아이콘(📋)으로 내용 전체 복사
2. Supabase 왼쪽 메뉴에서 **SQL Editor** 클릭 → **New query**
3. 복사한 내용 붙여넣기 → 오른쪽 아래 **Run** 클릭
4. "Success. No rows returned" 라고 나오면 성공

**✅ 확인**: 왼쪽 메뉴 **Table Editor**에 들어가면 테이블 목록이 보임

---

## 5단계. 나만의 도메인 연결하기 (선택, 10분)

`xxx.vercel.app` 주소로도 충분히 운영할 수 있습니다. `내서비스.com` 같은 주소를 원할 때만 진행하세요.

1. 도메인 구매: 가비아(gabia.com) 또는 Vercel에서 직접 구매 (연 1~3만 원 수준)
2. Vercel 프로젝트 → **Settings** → **Domains** → 구매한 도메인 입력 → **Add**
3. 화면에 나오는 안내대로 도메인 구매처에서 DNS 설정 (Vercel에서 구매했다면 이 과정 없음)
4. 최대 몇 시간 후 연결 완료 (보통 10분 이내)

**✅ 확인**: 내 도메인 주소로 접속했을 때 서비스가 보임

---

## 🔐 웹 서비스 추가 설정 (필수)

회원이 로그인해서 쓰는 서비스는 **"내 데이터는 나만 볼 수 있게"** 하는 보안 설정이 반드시 필요합니다. 이걸 안 하면 회원 A가 회원 B의 데이터를 볼 수 있습니다.

### 데이터 보안 규칙(RLS) 켜기 (3분)

다행히 이것도 준비된 명령을 붙여넣기만 하면 됩니다.

1. GitHub 저장소에서 `supabase/rls_policies.sql` 파일 내용 전체 복사
2. Supabase → **SQL Editor** → **New query** → 붙여넣기 → **Run**
3. "Success" 확인

**✅ 확인**: Supabase → **Table Editor** → 아무 테이블 클릭 → 상단에 "RLS enabled" 초록 표시가 보임

⚠️ 이 설정 전에는 실제 회원을 받지 마세요. 테스트는 괜찮습니다.

### 관리자 화면이 필요하다면 (선택)

회원과 데이터를 직접 관리할 화면은 **기획서에 넣어야 만들어집니다.** 자동으로 생기지 않습니다.

`docs/PRD.md`에 관리자 화면이 있는지 먼저 확인하세요.

- **있다면** — 거기 적힌 주소로 접속해 보세요. 접근 권한 설정도 기획서 내용을 따릅니다.
- **없다면** — 지금은 관리자 화면이 없는 게 정상입니다. 필요하면 큐비보라 대화창에서 기능으로 추가한 뒤 다시 만들면 됩니다.

---

# 🔓 실전 전환 가이드 (연습 모드 → 실제 서비스)

지금까지 내 서비스는 **연습 모드**(`USE_MOCK_SANDBOX=true`)로 돌아갔습니다. 결제·로그인 등이 가짜로 동작해서, 실제 키 없이도 전체 흐름을 확인할 수 있었습니다.

이제 실제 손님을 받을 준비가 됐다면 아래를 진행하세요.

> ⚠️ **전환 전 체크**: 연습 모드에서 처음부터 끝까지 한 번 사용해보고, 화면·흐름에 문제가 없는지 먼저 확인하세요. 실전 전환은 되돌릴 수 있지만, 문제가 있는 상태로 손님을 받으면 안 됩니다.

진행 순서: **① 아래 해당 항목의 실제 키 발급 → ② Vercel에 키 등록 → ③ 스위치 끄기 → ④ 최종 테스트**

---

## ① -A. 소셜 로그인 실제 연동 (15분)

연습 모드에서는 아무 이메일로나 로그인됐지만, 실전에서는 카카오/구글 실계정으로 로그인됩니다.

### 카카오 로그인

1. developers.kakao.com 로그인 → **내 애플리케이션** → **애플리케이션 추가하기**
2. 앱 이름·회사명 입력 (개인은 본인 이름) → 저장
3. 만든 앱 클릭 → **앱 키** 화면의 **REST API 키** 복사 → 메모장에 저장
4. 왼쪽 메뉴 **카카오 로그인** → **활성화 설정** ON
5. **Redirect URI 등록** 클릭 → 아래 주소 입력:
   `https://[내 Supabase URL]/auth/v1/callback`
   (2단계에서 저장한 Project URL 뒤에 `/auth/v1/callback`을 붙인 것)
6. Supabase → **Authentication** → **Providers** → **Kakao** 켜기 → 3번의 REST API 키 붙여넣기 → Save

**✅ 확인**: Supabase Providers 화면에서 Kakao가 초록색(Enabled)

💡 구글 로그인도 방식은 같습니다 — console.cloud.google.com에서 OAuth 클라이언트를 만들고 Supabase Providers의 Google에 등록합니다. 막히면 🆘 막혔어요 버튼에 "구글 로그인 연동 중 [상황]"이라고 보내주세요.



---

## ② 발급받은 키를 Vercel에 등록 (5분)

1. vercel.com → 내 프로젝트 → **Settings** → **Environment Variables**
2. ①에서 발급받은 키를 하나씩 추가합니다. **Key 이름은 저장소 `mocks/README.md`의 환경변수 표와 정확히 같아야 합니다** (대소문자 포함)
3. 전부 추가했으면 다음 단계로

---

## ③ 연습 모드 스위치 끄기 (1분)

1. 같은 Environment Variables 화면에서 `USE_MOCK_SANDBOX` 찾기
2. 오른쪽 ⋯ 메뉴 → **Edit** → 값을 `true` → **`false`** 로 변경 → Save
3. 상단 **Deployments** 탭 → 가장 최근 배포 오른쪽 ⋯ → **Redeploy** 클릭 (환경변수 변경은 재배포해야 적용됩니다)
4. 2~3분 후 재배포 완료

---

## ④ 최종 실전 테스트

- [ ] 실제 카카오/구글 계정으로 로그인이 되는가
- [ ] (결제가 있다면) 본인 카드로 최소 금액 결제 → 결제사 대시보드에 내역 확인 → 취소 처리까지
- [ ] 외부 기능(지도·AI 등)이 실제 데이터로 동작하는가
- [ ] 처음 방문자 입장에서 가입부터 핵심 기능까지 한 바퀴

**전부 통과했다면 — 🎉 축하합니다. 내 서비스가 실전 운영을 시작했습니다.**

❌ 어느 하나라도 실패하면: 실패한 화면의 에러 메시지(또는 캡처)를 🆘 막혔어요 버튼으로 보내주세요. 어느 단계(①~④)에서 막혔는지 함께 적으면 더 빨리 해결됩니다.

> 🔙 **되돌리기**: 문제가 심각하면 `USE_MOCK_SANDBOX`를 다시 `true`로 바꾸고 Redeploy 하세요. 즉시 연습 모드로 돌아가며, 그동안 서비스는 계속 떠 있습니다.
